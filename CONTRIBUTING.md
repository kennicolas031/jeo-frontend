# Contribution Rules

1. Do all work inside `dev` branch.
2. Never push to `main`.
3. Run `npm install` before starting.
4. Commit format:
   feat: new component added  
   fix: bug fix  
   refactor: cleaned code  
